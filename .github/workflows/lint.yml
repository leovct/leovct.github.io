name: lint

on:
  pull_request:
  push:
    branches: [main]

env:
  PNPM_VERSION: '10.16.1' # https://www.npmjs.com/package/pnpm

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install dependencies
        run: pnpm install

      - name: Run linter
        run: pnpm run check
